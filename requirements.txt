# Direct dependencies only, pinned exactly. Transitive versions (numpy, scipy)
# are left to the resolver — pinning the full tree belongs in a lockfile, and
# CI reproducibility here is served by pinning what the code actually imports.
#
# Phase 1 (model core) needs these. FastAPI and its dependencies arrive in
# Phase 2, deliberately not earlier.

pandas==3.0.5
scikit-learn==1.9.0
joblib==1.5.3

# Tooling — pytest is required by Phase 1, ruff by Phase 4 CI. Both are pinned
# so a new lint rule in a future release cannot turn a green build red without
# a deliberate version bump.
pytest==9.1.1
ruff==0.16.0
